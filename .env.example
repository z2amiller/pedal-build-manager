# Copy to .env and fill in before running `docker compose up`

# Admin password for /admin/* routes (required)
ADMIN_PASSWORD=changeme

# Public domain name for Caddy TLS (required)
# Caddy will auto-provision a Let's Encrypt certificate for this domain.
DOMAIN=builds.example.com
